# Core Settings
PYTHONPATH="."

# API Keys (OPTIONAL - can be provided via frontend UI)
# Users can enter their API keys directly in the web interface
# Set these only if you want to use server-side keys instead
GOOGLE_API_KEY=""
SEC_HEADER=""

# API Configuration
API_PORT=8000
FRONTEND_PORT=3000
